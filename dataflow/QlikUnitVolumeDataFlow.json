{
	"name": "QlikUnitVolumeDataFlow",
	"properties": {
		"type": "MappingDataFlow",
		"typeProperties": {
			"sources": [
				{
					"dataset": {
						"referenceName": "QlikUnitVolume",
						"type": "DatasetReference"
					},
					"name": "source1"
				}
			],
			"sinks": [
				{
					"dataset": {
						"referenceName": "QlikUnitVolumeSQL",
						"type": "DatasetReference"
					},
					"name": "sink1"
				}
			],
			"transformations": [
				{
					"name": "derivedColumn1",
					"description": "Universal dolar sign removal from all cells"
				}
			],
			"scriptLines": [
				"source(output(",
				"          Store as string,",
				"          Region as string,",
				"          {Month Year} as string,",
				"          Date as string,",
				"          Status as string,",
				"          {Deal Type} as string,",
				"          Refer as string,",
				"          {Deal No} as string,",
				"          Jrnl as string,",
				"          {Sales Mgr} as string,",
				"          {F&I Mgr} as string,",
				"          {Last Name} as string,",
				"          {First Name} as string,",
				"          City as string,",
				"          State as string,",
				"          ZIP as string,",
				"          Make as string,",
				"          Model as string,",
				"          {Model Yr} as string,",
				"          VIN as string,",
				"          Miles as string,",
				"          {Sale Type} as string,",
				"          {Buy Rate} as string,",
				"          {Sell Rate} as string,",
				"          {Days In Stk} as string,",
				"          Appraiser as string,",
				"          {Units ()} as string,",
				"          {Sales ()} as string,",
				"          {Cost ()} as string,",
				"          {Incentives ()} as string,",
				"          {Front ()} as string,",
				"          {FI $ ()} as string,",
				"          {VSC $ ()} as string,",
				"          {PPM $ ()} as string,",
				"          {GAP $ ()} as string,",
				"          {APPEAR $ ()} as string,",
				"          {KEY $ ()} as string,",
				"          {RHT $ ()} as string,",
				"          {DENT $ ()} as string,",
				"          {KAHU/LOJACK $ ()} as string,",
				"          {AFTERMARKET $ ()} as string,",
				"          {Back ()} as string,",
				"          {Deal ()} as string,",
				"          {Trade Allow ()} as string,",
				"          {Trade ACV ()} as string,",
				"          {Trade O/U ()} as string,",
				"          {List Price} as string,",
				"          {Trade 1 VIN} as string,",
				"          {Trade 2 VIN} as string,",
				"          {Avg Payment ()} as string,",
				"          fidealtype as string,",
				"          dealtype_orig as string,",
				"          slsdealtype as string,",
				"          salesmanagementdealtype as string,",
				"          crmsaletype as string",
				"     ),",
				"     allowSchemaDrift: false,",
				"     validateSchema: false,",
				"     ignoreNoFilesFound: false) ~> source1",
				"source1 derive(each(match(1==1), $$ = replace(trim($$), '$', ''))) ~> derivedColumn1",
				"derivedColumn1 sink(allowSchemaDrift: false,",
				"     validateSchema: false,",
				"     input(",
				"          Store as string,",
				"          Region as decimal(18,2),",
				"          {Month Year} as string,",
				"          Date as date,",
				"          Status as string,",
				"          {Deal Type} as string,",
				"          Refer as string,",
				"          {Deal No} as decimal(18,2),",
				"          Jrnl as string,",
				"          {Sales Mgr} as string,",
				"          {F&I Mgr} as string,",
				"          {Last Name} as string,",
				"          {First Name} as string,",
				"          City as string,",
				"          State as string,",
				"          ZIP as string,",
				"          Make as string,",
				"          Model as string,",
				"          {Model Yr} as decimal(18,2),",
				"          VIN as string,",
				"          Miles as decimal(18,2),",
				"          {Sale Type} as string,",
				"          {Buy Rate} as decimal(18,2),",
				"          {Sell Rate} as decimal(18,2),",
				"          {Days In Stk} as decimal(18,2),",
				"          Appraiser as string,",
				"          {Units ()} as decimal(18,2),",
				"          {Sales ()} as decimal(18,2),",
				"          {Cost ()} as decimal(18,2),",
				"          {Incentives ()} as decimal(18,2),",
				"          {Front ()} as decimal(18,2),",
				"          {FI $ ()} as decimal(18,2),",
				"          {VSC $ ()} as decimal(18,2),",
				"          {PPM $ ()} as decimal(18,2),",
				"          {GAP $ ()} as decimal(18,2),",
				"          {APPEAR $ ()} as decimal(18,2),",
				"          {KEY $ ()} as decimal(18,2),",
				"          {RHT $ ()} as decimal(18,2),",
				"          {DENT $ ()} as decimal(18,2),",
				"          {KAHU/LOJACK $ ()} as decimal(18,2),",
				"          {AFTERMARKET $ ()} as decimal(18,2),",
				"          {Back ()} as decimal(18,2),",
				"          {Deal ()} as decimal(18,2),",
				"          {Trade Allow ()} as decimal(18,2),",
				"          {Trade ACV ()} as decimal(18,2),",
				"          {Trade O/U ()} as decimal(18,2),",
				"          {List Price} as decimal(18,2),",
				"          {Trade 1 VIN} as string,",
				"          {Trade 2 VIN} as string,",
				"          {Avg Payment ()} as decimal(18,2),",
				"          fidealtype as string,",
				"          dealtype_orig as string,",
				"          slsdealtype as string,",
				"          salesmanagementdealtype as string,",
				"          crmsaletype as decimal(18,2)",
				"     ),",
				"     deletable:false,",
				"     insertable:true,",
				"     updateable:false,",
				"     upsertable:false,",
				"     format: 'table',",
				"     skipDuplicateMapInputs: true,",
				"     skipDuplicateMapOutputs: true,",
				"     mapColumn(",
				"          each(match(/* All input columns */true()),",
				"               /* Input name */$$ = $$)",
				"     )) ~> sink1"
			]
		}
	}
}